syntax = "proto3";

import "google/protobuf/Empty.proto";
import "ProtoMessages/Common.proto";
import "GestorContact.Grpc/Admin/Messages.proto";

package gestorContact.Grpc;

service GestorContactAdmin {
	rpc ListContacts (google.protobuf.Empty) returns (ContactMessageList);
	rpc GetContact (protoMessages.Int32Message) returns (ContactResponse);
	rpc CreateContact (ContactMessage) returns (ContactResponse);
	
}
